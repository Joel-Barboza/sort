

export const Bar = ({ value }) => {
    return (
        <div 
            className="bar"
            style={{height: `${ value * 5 + 3 }px`}}
        >
            <p>{  }</p>
        </div>
    )
}
